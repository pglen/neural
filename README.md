# Toying around with neurons

## No useful content

 The idea we are exploring is that we connect multiple ins with
multiple outs. The input pattern that is closest to the trainig set,
(or is matched), an output pattern is emitted.

# EOF